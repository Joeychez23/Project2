
<div>
    <h2><a href="/portfolio/{{currId}}">{{currUser}}</a>'s Following:</h2>
</div>
{{#each users as |user|}}
<div class="row defaultRowClass">
  <div class="container-fluid postPreview">
    <div class="row">
      <div class="col-md-12">
        <h4><a href="/portfolio/{{id}}">{{user.username}}</a></h4>
      </div>
    </div>
  </div>

</div>

{{/each}}